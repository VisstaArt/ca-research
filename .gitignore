.DS_Store

# Файлы с ключами и доступами. Репозиторий ПУБЛИЧНЫЙ, а эти файлы лежали
# незакоммиченными рядом с кодом — одного «git add -A» хватило бы, чтобы они
# навсегда попали в открытую историю. Из истории git такое не вычищается
# простым удалением файла.
*.env
api/*.env
api/*.rtf
