ObjC.import('Foundation');
// Строгий формат офферов и гипотез. Сухой прогон: вход для офферов,
// мастерская, готовые офферы, доска гипотез, ставка и цели.
function readFile(p){return $.NSString.stringWithContentsOfFileEncodingError($(p),$.NSUTF8StringEncoding,null).js;}
var ROOT=$.NSFileManager.defaultManager.currentDirectoryPath.js;
var SRC=readFile(ROOT+'/app.jsx');
var стенд=readFile(ROOT+'/tests/dom-stub.js');
eval(readFile(ROOT+'/tests/render-harness.js'));
function взять(n){var i=SRC.indexOf('async function '+n+'('); if(i<0) i=SRC.indexOf('function '+n+'(');
 if(i<0) throw new Error('нет '+n);
 var d=0; for(var k=SRC.indexOf('{',i);k<SRC.length;k++){if(SRC[k]==='{')d++;else if(SRC[k]==='}'){d--;if(!d)return SRC.slice(i,k+1);}}}
console.log('tests/strict-m7.test.js');
var fails=0;
function ok(n,g,w){var r=JSON.stringify(g)===JSON.stringify(w);
 if(!r){fails++;console.log('  FAIL '+n+' | ждали '+JSON.stringify(w)+' | факт '+JSON.stringify(g));}
 else console.log('  ok   '+n);}

eval(взять('safeJson'));
globalThis.blockScriptsM3=[];
eval(взять('разметкаM7')); eval(взять('текстИзСтрогогоM7'));
var КОД=renderResearchHTML('', {}).js;

function собрать(д){
  blockScriptsM3=[];
  var html=разметкаM7(д,null);
  var упало=[], пустые=[];
  blockScriptsM3.forEach(function(скрипт){
    try { new Function(стенд+'\n'+КОД+'\n'+скрипт)(); }
    catch(e){ упало.push(скрипт.slice(0,26)+' → '+e.message); }
  });
  var ящики=(html.match(/id="(rpt-[^"]+)"/g)||[]).map(function(x){return x.slice(4,-1);});
  try {
    var f=new Function(стенд+'\n'+КОД+'\n'+blockScriptsM3.join('\n')
      +'\nreturn '+JSON.stringify(ящики)+'.filter(function(и){'
      +'var э=document.getElementById(и); return !э || (!String(э.innerHTML).trim() '
      +'&& !(э.children && э.children.length));});');
    пустые=f();
  } catch(e){ упало.push('проверка ящиков: '+e.message); }
  return { html: html, упало: упало, пустые: пустые };
}

var д={гипотезы:[],что_знаем_о_продавце:[],черновые_офферы:[],готовые_офферы:[],ставка:[],цели:[],
  итог:{что_узнали:['ставим на скорость'],что_это_значит:['обещаем день'],что_делаем:['тест на лендинге']}};
var поля=['Компетенции','Отличия','Доказательства','Кейсы','Гарантии','Цены'];
поля.forEach(function(п,i){
  д.что_знаем_о_продавце.push({поле:п,значение:i<4?'что-то известное':'',
    откуда:i<2?'из брифа':(i<4?'с сайта':'не задано')});
});
for (var i=1;i<=12;i++){
  д.гипотезы.push({id:'H'+i,персона:'Владелец',формулировка:'Если показать N, то вырастет M',
    что_тестируем:'заголовок',ожидаемый_эффект:'+10% заявок',метрика:'конверсия',
    дизайн_теста:'A/B две недели',риски:'мало трафика',
    статус:['ждёт проверки','в работе','проверена'][i%3],уверенность:(i%5)+1});
}
for (var j=1;j<=6;j++){
  д.черновые_офферы.push({id:'O'+j,персона:'Владелец',осведомлённость:'ищет решение',
    хуки:['«Теряете половину заявок?»','«Верните ушедших»','«Заявка вместо ухода»'],
    боль:'уходят без заявки',результат:'на 12% больше заявок',
    механизм:'ловим момент ухода',доказательства:'кейс магазина мебели'});
}
for (var k=1;k<=2;k++){
  д.готовые_офферы.push({сегмент:'Интернет-магазины',осведомлённость:'сравнивает продукты',
    заголовок:'Верните тех, кто уходит',подзаголовок:'Виджет ловит момент ухода и предлагает то, что смотрели',
    проблема:'посетители уходят молча',результат:'больше заявок с того же трафика',
    состав:['установка за день','готовые сценарии','отчёт по неделям'],
    цена:'от 1 990 ₽/мес',снятие_риска:k===1?'14 дней бесплатно':'',
    призыв:'Попробовать бесплатно',куда_ведёт:'форма заявки',
    ценность:{желанный_результат:'на 12% больше заявок с того же трафика',
      вера_что_получится:'кейс магазина мебели и демо на своём сайте',
      сколько_ждать:'первые показы в день установки',
      сколько_усилий:'вставить одну строку кода, сценарии готовы',
      слабое_место:'вера: кейс пока один, нужен второй в другой категории'}});
}
['Ставка','На чём стоит','Если верна','Если нет','Чем проверяем'].forEach(function(э,i){
  д.ставка.push({элемент:э,формулировка:'Формулировка '+(i+1)});
});
for (var m=1;m<=3;m++){
  д.цели.push({цель:'Цель '+m,метрика:'конверсия',сейчас:m%2?'не замерено':'2,1%',
    цель_значение:'3%',какие_гипотезы:'H1, H4'});
}

д.декомпозиция={цель_в_деньгах:'1 200 000 ₽ за квартал',средний_чек:'24 000 ₽ (из брифа)',
  нужно_продаж:'50 сделок',конверсия_в_продажу:'не замерено',нужно_заявок:'≈250 заявок при 20%',
  конверсия_в_заявку:'не замерено',нужно_посетителей:'≈12 500 при 2%',
  откуда_числа:'чек из брифа, конверсии не замерены — взяты как ориентир',
  что_проверить:'конверсию из заявки в продажу: на ней держится вся цепочка'};

var р=собрать(д);
ok('ни одна рисовалка не падает', р.упало, []);
ok('ни один блок не остался пустым', р.пустые, []);
ok('сырых таблиц нет', (р.html.match(/<table/g)||[]).length, 0);
var с=blockScriptsM3.join(' ');
ok('вход для офферов собран', /renderOfferInput\(/.test(с), true);
ok('мастерская собрана', /renderWorkbench\(/.test(с), true);
ok('готовые офферы собраны', (с.match(/renderFinalOffer\(/g)||[]).length, 2);
ok('доска гипотез собрана', /renderKanban\(/.test(с), true);
ok('главная ставка собрана', /renderBet\(/.test(с), true);
ok('цели собраны', /renderCriteria\(/.test(с), true);
ok('про незаполненные поля сказано', /Незаполненных полей: 2/.test(р.html), true);

// Оффер без снятия риска не выдаёт пустое место за гарантию.
var строкиОфферов=blockScriptsM3.filter(function(x){return x.indexOf('renderFinalOffer(')===0;});
var второй=строкиОфферов[1];
var данныеВторого=JSON.parse(второй.slice('renderFinalOffer('.length, второй.lastIndexOf(',')));
ok('пустое снятие риска — null, а не пустая строка', данныеВторого.risk, null);
// Хуки приходят списком, а не одной склеенной строкой.
var строкаWB=blockScriptsM3.filter(function(x){return x.indexOf('renderWorkbench(')===0;})[0];
var wb=JSON.parse(строкаWB.slice('renderWorkbench('.length,-2));
ok('хуки списком', Array.isArray(wb[0][1]) && wb[0][1].length, 3);

// Разбор ценности: четыре части и честно названное слабое место.
ok('разбор ценности напечатан', /Из чего складывается ценность/.test(р.html), true);
ok('слабое место названо', /слабое место/.test(р.html), true);
ok('все четыре части на месте',
   ['что получит','почему поверит','через сколько','что делать клиенту']
     .every(function(п){ return р.html.indexOf(п)>0; }), true);

// Декомпозиция цели: от денег обратно к трафику, с пометкой, откуда числа.
ok('декомпозиция напечатана', /Что нужно, чтобы дойти до цели/.test(р.html), true);
ok('видно, откуда числа', /Откуда числа/.test(р.html), true);
ok('названо, что проверить первым', /Что проверить первым/.test(р.html), true);

var текст=текстИзСтрогогоM7(д);
ok('в выжимке есть офферы', /Верните тех, кто уходит/.test(текст), true);
ok('в выжимке есть итог', /ИТОГ МОДУЛЯ/.test(текст), true);

var пусто={гипотезы:[],что_знаем_о_продавце:[],черновые_офферы:[],готовые_офферы:[],
  ставка:[],цели:[],итог:{что_узнали:[],что_это_значит:[],что_делаем:[]}};
var р2=собрать(пусто);
ok('на пустых данных не падает', р2.упало, []);
ok('на пустых данных разметки нет вовсе', р2.html, '');
console.log(fails?('ПРОВАЛЕНО: '+fails):'  всё зелёное');
