var fails=0;
function check(n,g,w){var ok=JSON.stringify(g)===JSON.stringify(w);
 if(!ok){fails++;console.log('  FAIL '+n+' | ждали '+JSON.stringify(w)+' | факт '+JSON.stringify(g));}
 else console.log('  ok   '+n);}
function H(id){return document.getElementById(id).innerHTML||'';}

renderJtbd([['Владелец магазина','заявок стало меньше','понять, где теряю','не платить за трафик зря','рост заявок на треть','нет аналитики форм','что сольют базу']]);
var j=H('rpt-jtbd');
check('JTBD: одна карточка', (j.match(/rule-card/g)||[]).length, 1);
check('JTBD: фраза собрана', /Когда<\/i> заявок стало меньше, <i>я хочу<\/i> понять, где теряю, <i>чтобы<\/i> не платить/.test(j), true);
check('JTBD: успех, пробел и страх не потерялись',
  /Успех: рост заявок на треть · Пробел: нет аналитики форм · Страх: что сольют базу/.test(j), true);

renderTactics([['Социальное доказательство','принцип подражания','С нами уже 300 магазинов','нельзя показывать чужие логотипы без согласия','Владелец · отзывы · CR']]);
var t=H('rpt-tactics');
check('Тактики: риск отдельной строкой', (t.match(/class="warnline"/g)||[]).length, 1);
check('Тактики: пример в кавычках', /«С нами уже 300 магазинов»/.test(t), true);

renderPositioning([['Интернет-магазины','сервис возврата брошенных корзин','возвращает до 12% ушедших','не виджет, а сценарий','против: ничего не делать · доказываем: замер на 40 сайтах']]);
check('Позиционирование: категория как имя', /class="nm">сервис возврата брошенных корзин/.test(H('rpt-pos3')), true);

renderArchetype([['Заботливый','компания','объясняем сложное простыми словами','у соперников — Герой','можем звучать мягко там, где нужен нажим']]);
var a=H('rpt-arch');
// Карточки архетипов равной ширины: широкая первая и узкая вторая читались
// как сбой вёрстки (владелица 15.09), а роль подписана сверху словами.
check('Архетип: карточки равной ширины', /class="rule-card"/.test(a) && !/ wide/.test(a), true);
check('Архетип: занято и риск в подписи', /У соперников занято: у соперников — Герой · Рискуем: можем звучать мягко/.test(a), true);

renderIntent([['Попапы для сайта','попап для сайта, всплывающее окно','лендинг · L3 · 12 000/мес','Верните тех, кто уходит','Разбор на 40 сайтах']]);
var ic=H('rpt-intent');
check('Кластеры: шапка на месте', (ic.match(/class="th"/g)||[]).length, 1);
check('Кластеры: метка роли и осведомлённости', /class="tag">лендинг · L3 · 12 000\/мес/.test(ic), true);

renderCriteria([['Скорость внедрения','Владелец · нет времени на интеграцию','запуск за день','видео установки 3 минуты','лендинг, блок FAQ']]);
check('Критерии: где показываем не потеряно', /лендинг, блок FAQ/.test(H('rpt-crit')), true);

renderWorkbench([['O-1',['Верните брошенные корзины','Не теряйте тех, кто ушёл','12% возвращаются сами'],'уходят без заявки','возврат части ушедших','триггер по времени на странице','замер на 40 сайтах','Владелец · L3']]);
var w=H('rpt-wb');
check('Мастерская: три хука списком', (w.match(/<li>/g)||[]).length, 3);
check('Мастерская: боль и результат', /Боль: уходят без заявки · Результат: возврат части ушедших/.test(w), true);

renderBenchmarks([['Просмотры ролика','9 800','1 200–52 000','14 единицах',''],['Комментарии','34','4–210','','редко у постов']]);
var b=H('rpt-bmk');
check('Бенчмарки: две плитки', (b.match(/class="bcell"/g)||[]).length, 2);
check('Бенчмарки: объём выборки виден', /на 14 единицах/.test(b), true);
check('Бенчмарки: без объёма — честная пометка', /объём не указан/.test(b), true);



console.log(fails? '\nПРОВАЛОВ: '+fails : '\nвсё сошлось');
