# Дизайн-система платформы

Решение владелицы 06.09.2026. Прототип, на котором выбирали:
https://claude.ai/code/artifact/8a9e2dbc-965e-48f1-a17e-a8c20d667061

Оболочку строим на этих значениях. Не менять их по ходу написания кода —
если что-то не сходится, сначала обсудить, потом править здесь.

## Акцент — Тиффани

| Токен | Светлая | Тёмная | Что это |
|---|---|---|---|
| `--clay` | `#0ABAB5` | `#A6D8D5` | основной акцент |
| `--clay-deep` | `#16A39F` | `#0ABAB5` | нажатое состояние, наведение |

В тёмной теме акцент СВЕТЛЕЕ, а не тот же самый: на тёмном фоне исходный
тонет. Это относится и к шкале ниже — она разворачивается.

**Почему прошёл именно этот оттенок.** Узнаваемый светлый «подарочный»
Тиффани `#81D8D0` проверку НЕ прошёл: контраст к фону 1.57 и насыщенность
ниже порога — глаз читает его как серый. Классический `#0ABAB5` проходит.

**Про товарный знак.** Цвет Тиффани зарегистрирован (Pantone 1837), но
цветовые знаки защищают КАТЕГОРИЮ — ювелирку и её розницу. Для сервиса
маркетинговых исследований риск практически нулевой. Это не юридическое
заключение; если цвет станет основой всего бренда — стоит разовой проверки.

## Шкала акцента — для величин и порядковых шкал

| Токен | Светлая | Тёмная |
|---|---|---|
| `--a1` | `#F6FAFA` | `#86ADAB` |
| `--a2` | `#DAEDEC` | `#A6D8D5` |
| `--a3` | `#A2D6D4` | `#CCE7E5` |
| `--a4` | `#0ABAB5` | `#E9F4F3` |
| `--a5` | `#1B9591` | `#F9FCFC` |

Шкала монотонна по светлоте — соседние ступени различимы, в том числе в
оттенках серого. Строилась смешиванием с белым и тёмным В ЛИНЕЙНОМ
пространстве: при обычном смешивании середина мутнеет, а бирюза уходит
в кислотную.

**Где применяется:** риск потери на этапах пути клиента, вердикт ниши
(Go / Maybe / No-Go), любые порядковые шкалы. Это НЕ состояния, а величины,
поэтому один тон от светлого к тёмному, а не три разных цвета.

## Статусы — только для состояний

| Токен | Светлая | Тёмная | Подложка светлая | Подложка тёмная |
|---|---|---|---|---|
| `--go` | `#0E8A5F` | `#9EBDAD` | `#EAEDE7` | `#1C4631` |
| `--maybe` | `#BE9722` | `#D7C3A0` | `#F3EEE6` | `#5F4C1C` |
| `--no` | `#A6384E` | `#CAA3A8` | `#F1EAE7` | `#53242A` |

Холодный набор — под холодный акцент. Есть и тёплый (`#4F7A21` / `#C8922A` /
`#8F3A26`) на случай смены акцента на тёплый.

**Правило:** статусы — для «работает / сломалось», не для шкал. И никогда
не цветом в одиночку: рядом всегда слово.

**Осторожно при правках:** тёплый вариант с жжёной охрой проверку ПРОВАЛИЛ —
охра и кирпич неразличимы даже нормальным зрением, а с зелёным сливаются при
дальтонизме. Любую замену статусных цветов гонять через
`dataviz/scripts/validate_palette.py`, а не подбирать на глаз.

## Нейтральные

| Токен | Светлая | Тёмная | Что это |
|---|---|---|---|
| `--bone` | `#F0EEE6` | `#171614` | фон страницы |
| `--paper` | `#FAF9F5` | `#1F1D1A` | карточки, боковое меню |
| `--raise` | `#FFFFFF` | `#26241F` | активный пункт меню |
| `--ink` | `#141413` | `#EDEAE0` | основной текст |
| `--ink-2` | `#57544C` | `#B5B0A3` | вторичный |
| `--ink-3` | `#8C887C` | `#84806F` | подписи, приглушённое |
| `--line` | `#E1DED2` | `#332F28` | границы |
| `--line-2` | `#EDEAE0` | `#2A2721` | внутренние разделители |

Фон НЕ чистый белый, текст НЕ чистый чёрный — это единственная часть
цветовых решений, за которой стоят исследования, а не вкус: предельный
контраст утомляет глаз при долгой работе и даёт эффект ореола, особенно
людям с астигматизмом.

**Открытый вопрос:** нейтральные тёплые (костяные), а акцент холодный.
Сочетание рабочее, но если понадобится больше единства — фон можно охладить.
Решаем, когда посмотрим вживую на нескольких экранах.

## Шрифты — Монсеррат + Source Serif 4

| Токен | Значение |
|---|---|
| `--sans` | `Montserrat, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif` |
| `--serif` | `'Source Serif 4', Georgia, 'Times New Roman', serif` |

Гротеск — интерфейс, заголовки, цифры. Антиква — текст отчёта и длинные
абзацы.

**ТРИ ЯЗЫКА — обязательное требование.** Оба шрифта проверены напрямую у
Google Fonts на латиницу (английский), расширенную латиницу (турецкие
ğ ş ı İ — они в диапазоне U+0100–02BA) и кириллицу.

**Не брать вслепую:** DM Sans, Newsreader и Archivo кириллицы не имеют
ВОВСЕ. Русский текст в них молча подменяется системным шрифтом, и выглядит
это как «шрифт не нравится», хотя он просто не применился. Любой новый шрифт
проверять на все три набора до того, как ставить в макет.

## Прочее

- **Ширина:** во всю ширину окна. Боковое меню 264px, содержимое тянется.
  Потолок 1500px, чтобы строки не расползались на широких экранах.
- **Адаптив:** до 980px меню уезжает наверх горизонтальной лентой.
- **Цифры:** везде `font-variant-numeric: tabular-nums`, иначе колонки
  чисел пляшут.
- **Тема:** три состояния — светлая, тёмная, системная. Токены задаются в
  `:root`, переопределяются в `@media (prefers-color-scheme: dark)` с
  защитой `:root:not([data-theme="light"])` и в `:root[data-theme="dark"]`.
  Иначе при системной теме (когда атрибут не проставлен) страница
  показывает текст одной темы на фоне другой.

## Структура навигации

Взята у контент-машины целиком — она продумана лучше моей первой версии:
контекст проекта сверху с возвратом к списку, группы «Начало проекта /
Настройки / Работа», счётчики на пунктах, где ждут решения человека.
