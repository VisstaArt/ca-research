# Себестоимость исследования

Пересчитано 18.09.2026. Заменяет прежний расчёт от 29.08 — он был сделан до
перенумерации модулей, до появления модуля «Аудитория» и до того, как
выяснилось, сколько поиска модули тратят на самом деле.

**Основа файла — ЗАМЕР с живого прогона** (кнопка «Расход» в интерфейсе,
18.09, ниша «Интернет-магазины»), а не расчёт. Прежняя версия этого файла
считалась по коду и промахнулась в обе стороны: занизила поиск почти вдвое и
завысила модели вдвое. Расчётом остались только строки, которых замер ещё не
касался, — они помечены.

Считаются обе статьи расхода: поисковая система и модель, по каждому модулю
отдельно.

---

## Цены, из которых считаем

| Статья | Цена | Откуда |
|---|---|---|
| Поисковый кредит | **$0,008** | тариф владелицы, 18.09 |
| Глубокий поиск | **2 кредита** за запрос | так списывает площадка |
| Обычный поиск | 1 кредит за запрос | там же |
| Лимит на один ключ | **1000 кредитов в месяц** | тариф владелицы |

Почти весь наш поиск — глубокий: он возвращает текст страницы, а не сниппет,
и без него модули строят выводы на заголовках. Поэтому **один наш запрос
стоит $0,016**.

Модели (за миллион токенов, по прейскуранту в `api/usage.js`):

| Модель | Вход | Выход | Где используется |
|---|---|---|---|
| `gpt-5.6-terra` | $2 | $12 | M1, M2, M4, M4A, M5, M8 |
| `gpt-5.6-sol` | $4 | $20 | M3, M6, M7 |

Выход дороже входа в шесть раз — поэтому на цену влияет не столько объём
материала, сколько **длина ответа**, то есть число полей в схеме модуля.

---

## Расход по модулям — ЗАМЕР

**Это настоящие числа**, снятые кнопкой «Расход» с прогона по нише
«Интернет-магазины» 18.09.2026. Они заменяют мой прежний расчёт: он врал, и
в обе стороны сразу.

| Модуль | Вход, т. | Выход, т. | Поисков | Модель | Поиск | **Всего** |
|---|---|---|---|---|---|---|
| M4A Аудитория | 13 916 | 863 | 94 | 4 ¢ | $1,50 | **$1,54** |
| M3 Конкуренты | 12 857 | 1 475 | 68 | 8 ¢ | $1,09 | **$1,17** |
| M4 Контент-радар | 21 879 | 13 725 | 50 | 21 ¢ | 80 ¢ | **$1,01** |
| M2 Разведка ниш | 30 934 | 6 599 | 44 | 14 ¢ | 70 ¢ | **85 ¢** |
| M5 Голос клиента | 14 758 | 4 516 | 38 | 8 ¢ | 61 ¢ | **69 ¢** |
| M6 Психология | 11 063 | 12 837 | 0 | 30 ¢ | 0 | **30 ¢** |
| **Итого шесть модулей** | **105 407** | **40 015** | **294** | **85 ¢** | **$4,70** | **$5,56** |

Здесь нет M1, M7, M8 и сборки отчёта. С ними полный прогон одной ниши —
**около $7**.

### Что в этих числах настоящее, а что наше

Вопрос владелицы 18.09: «это реальные наши расходы или система показывает то,
что сама же заранее проставила?» Ответ пополам, и разницу надо держать в уме.

| Столбец | Откуда берётся | Это факт? |
|---|---|---|
| Вход, выход, «думала» | присылает сам провайдер в ответе на каждый запрос | **да, факт** |
| Поисков | наш счётчик вызовов | **да**, но считает попытки |
| «Впустую» | вызовы, на которые поиск ответил ошибкой | **да, факт** |
| Модель, $ | наш пересчёт: токены × цена из прейскуранта | **нет, наш расчёт** |
| Поиск, $ | наш пересчёт: запросы × 1,6 ¢ | **нет, наш расчёт** |

То есть **количества настоящие, деньги — наш пересчёт по заложенным ценам.**
Если прейскурант в `api/usage.js` устарел или провайдер поменял тариф,
таблица будет считать по старому и молча врать.

**Поэтому раз в месяц сверяйте итог с кабинетами OpenAI и Tavily.** Разошлось —
правьте цены в `api/usage.js`, а не в отчётах.

Отдельно про «впустую»: за запрос, на который поиск ответил ошибкой,
кредит не списывается. Наш счётчик считает попытки, поэтому неудачи из денег
вычитаются — иначе прогон 18.09, где поиск не работал вовсе, показал бы
несколько долларов расхода вместо нуля.

### Что замер показал, а расчёт не видел

**1. Поиск — 85% расхода, а не две трети.** $4,70 против 85 ¢ на модели.
Всё, что касается экономии, начинается и заканчивается числом поисковых
запросов.

**2. Запросов почти вдвое больше, чем я считал.** 294 на шесть модулей против
моих 177–202 на девять. Мой холостой прогон шёл на скромном бриф-примере, а
на настоящем брифе списки длиннее: больше конкурентов, больше услуг, больше
сегментов. Особенно промахнулись «Конкуренты» — 68 запросов вместо 25.

**3. Модели ВДВОЕ дешевле, чем я считал.** 85 ¢ на шесть модулей против моих
$1,23 на девять. Я завышал объём материала: у «Конкурентов» насчитал 46 000
токенов входа, а на деле 12 857.

**4. Токенов рассуждения нет вовсе — везде ноль.** Моя догадка, что расход
съедают размышления модели, **не подтвердилась**. Хорошо, что проверили
замером, а не оставили в расчёте.

**5. Кредитов на прогон — около 600, а не 354.** 294 запроса по два кредита —
588 только на шесть модулей. Значит **лимита одного ключа (1000 кредитов)
хватает на ОДИН полный прогон, а не на два.** С тремя ключами — три прогона
в месяц.

### Самые дорогие места

| Место | Цена | Почему |
|---|---|---|
| M4A Аудитория | $1,54 | 94 поисковых запроса — больше всех |
| M3 Конкуренты | $1,17 | 68 запросов |
| M4 Контент-радар | $1,01 | 50 запросов плюс самый длинный ответ модели |

Модуль аудитории прогонялся на коде до урезания списка. На нынешнем коде его
запросов должно быть около 60 вместо 94 — это надо проверить следующим
замером.

---

## Сверх модулей — то, что тоже стоит денег

Модулями расход не заканчивается. Эти вызовы не привязаны к модулю и в
таблице выше их нет.

| Что | Когда | Модель | Цена |
|---|---|---|---|
| **Сборка отчёта** | по кнопке, можно пересобирать | terra | **$0,17 / $0,30 / $0,43** (1 / 2 / 3 ниши) |
| Разбор сайта в бриф | один раз при заведении проекта | terra | ~$0,03 |
| Ценовые слои после M1 | один раз | terra | ~$0,01 |
| Перевод отчёта | по кнопке, если нужен | terra | ~$0,15 за язык |
| Тренд-монитор | отдельный экран, не часть прогона | terra | ~$0,05 за снимок |

**Отчёт — самая дорогая из этих статей, и вот почему.** Ему кладут в промпт
результаты ВСЕХ модулей целиком, до 12 000 знаков с каждого. При одной нише
это девять секций, при трёх — двадцать три. Плюс сам отчёт длинный: до 8 000
токенов на одну нишу и до 24 000 на три.

Важно для тарифа: **отчёт пересобирается по кнопке**, и каждая пересборка
стоит столько же. Человек, который трижды переделал отчёт по трёхнишевому
исследованию, потратил $1,29 — больше, чем стоят два модуля.

---

## Сколько прогонов помещается в лимит

При 354 кредитах на прогон:

По замеру — около 600 кредитов на шесть модулей, порядка 700 на полный прогон:

| | Прогонов до исчерпания |
|---|---|
| Один ключ (1000 кредитов) | **1** |
| Два ключа (2000) | **2–3** |
| Три ключа (3000) | **4** |

Отсюда правило: **лимита одного ключа хватает на ОДИН полный прогон в месяц.**
Доводка модулей, когда один и тот же модуль перезапускается по десять раз,
съедает месячный лимит за день — так и случилось 17–18.09.

---

## Полная себестоимость одного исследования

| | Модели | Поиск | Отчёт и прочее | **Итого** |
|---|---|---|---|---|
| 1 ниша | ~$1,20 | ~$5,60 | $0,21 | **~$7,00** |
| 2 ниши | ~$2,20 | ~$10,90 | $0,34 | **~$13,40** |
| 3 ниши | ~$3,20 | ~$16,20 | $0,47 | **~$19,90** |

Одна ниша посчитана по замеру шести модулей ($5,56) плюс M1, M7, M8 и отчёт.
Две и три — от неё же: M1 и разведка ниш считаются один раз на рынок,
остальные семь модулей по каждой нише.

«Отчёт и прочее» — сборка отчёта плюс разбор сайта и ценовые слои, которые
считаются один раз на проект. Перевод и тренд-монитор сюда не входят: они по
требованию.

Две ниши дороже одной **почти вдвое, но не ровно**: M1 и разведка ниш
считаются один раз на рынок, остальные семь модулей — по каждой нише отдельно.

**Поиск — 85% себестоимости, модели — 15%.** По замеру, не по прикидке. Любая
экономия начинается и заканчивается числом поисковых запросов. Самый дорогой
по модели модуль — «Психология» (30 ¢): он не ищет вовсе, зато работает на
`sol` и пишет самый длинный ответ.

---

## Что с этим делать при расчёте тарифов

1. **Закладывайте $7,50 себестоимости на одну нишу** и $14 на две — с запасом на
   повторные прогоны и на то, что часть запросов возвращается пустыми, но
   всё равно оплачивается.
2. **Повторные прогоны — отдельная статья, и их две.** Перезапуск модуля:
   трижды перезапущенный контент-радар стоит ещё $2,61. Пересборка отчёта:
   трижды пересобранный отчёт по трём нишам — ещё $1,29. Либо это входит в
   подписку с ограничением по числу перезапусков, либо считается сверх.
3. **Лимит ключа — узкое место, а не деньги.** $8 за тысячу кредитов — это
   немного, но месячный потолок на ключ упирается в два прогона. Для нескольких
   клиентов нужен либо платный тариф без месячного потолка, либо несколько
   ключей с распределением нагрузки (сервер уже умеет перебирать ключи и
   сообщает, когда все исчерпаны).

---

## Как пересчитать, когда код изменится

Расход поиска задан в коде двумя местами:

- потолки в `gather*Evidence` (`app.jsx`) — сколько запросов модуль имеет
  право сделать;
- `ПОИСК_ПО_МОДУЛЮ` в смете (`app.jsx`) — те же числа для показа цены ДО
  прогона.

Они обязаны совпадать. Цена кредита и множитель за глубокий поиск —
в `api/usage.js` (`SEARCH_CENTS`, `SEARCH_DEPTH_FACTOR`); экран берёт их
оттуда, а не из своих констант.

Фактический расход каждого прогона пишется в результат модуля
(`searchCalls`, `keywordCalls`) — по нему смета уточняется сама, как только
накопятся настоящие прогоны.
